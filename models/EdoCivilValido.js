// Roles de usuarios para controlar acceso a los diferentes API's 
let edoCivilValido = {
    values: ['S', 'C'],
    message: '{VALUE} no es un ESTADO CIVÍL válido.'
}

module.exports = edoCivilValido;
